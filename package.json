{
  "name": "car-sensor-iot",
  "version": "1.0.0",
  "description": "Car sensor IoT monorepo with server, client and arduino packages",
  "private": true,
  "scripts": {
    "dev:server": "pnpm --filter ./packages/server run dev",
    "dev:client": "pnpm --filter ./packages/client run dev",
    "build": "pnpm --recursive build",
    "dev": "pnpm --recursive dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2",
    "tsx": "^3.12.10",
    "@types/node": "^20.6.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}